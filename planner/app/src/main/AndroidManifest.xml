<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alvaro.tfg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".EditPlan"></activity>
        <activity android:name=".info_plans" />
        <activity android:name=".my_plans" />
        <activity android:name=".listo" />
        <activity android:name=".compartir" />
        <activity android:name=".CrearPlan" />
        <activity android:name=".registro" />
        <activity android:name=".MainActivity" />
        <activity android:name=".principal" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
